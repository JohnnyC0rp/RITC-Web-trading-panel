{
  // DMA (remote) REST API endpoint
  "base_url": "http://flserver.rotman.utoronto.ca:10001",

  // Client (local) REST API endpoint (RIT User App must be running)
  "client_base_url": "http://localhost:9999",

  // Trader credentials for DMA
  "username": "YOUR_TRADER_ID",
  "password": "YOUR_PASSWORD",

  // Optional: precomputed Basic Authorization header
  // "authorization_header": "Basic <BASE64(user:pass)>",

  // Client REST API key from the RIT app
  "api_key": "YOUR_API_KEY"
}
